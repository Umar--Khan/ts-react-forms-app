const ERROR_MESSAGES = {
  requiredField: "This field is required",
  positiveAge: "Age needs to be positive",
  evenAge: "Weird age...it needs to be a even number",
};

enum GENDER_ENUM {
  MALE = "male",
  FEMALE = "female",
}

export { ERROR_MESSAGES, GENDER_ENUM };
